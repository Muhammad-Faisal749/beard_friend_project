

import 'dart:ui';

class ProfileSelectionData {
  String ?image;
  String?text;
  Color? color;
   bool ? isText;
   bool?isimage;
  ProfileSelectionData({this.image,this.text,this.color,this.isText,this.isimage});
}